$("body").prepend("<div id='success'><%= @venue_name %> deleted.</div>");
var check = "<%= @season %>";
if (check.length){
  retrieveSeasonTeams(check);//refresh teams in season
}
$("#static-venueslist").load(location.href + " #infoc").fadeIn('normal', function(){
  $("#calendar-view").fadeIn();
  $("#season-info").fadeIn();
  $("#team-info").empty();
});

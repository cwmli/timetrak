$("body").prepend("<%= j content_tag(:div, 'Team deleted.', id: 'success') %>");
var check = "<%= @season %>";
if (check.length){
  retrieveSeasonTeams(check);//refresh teams in season
}
$("#static-teamslist").load(location.href + " #infob").fadeIn('normal', function(){
  $("#calendar-view").fadeIn();
  $("#season-info").fadeIn();
  $("#team-info").empty();
});
